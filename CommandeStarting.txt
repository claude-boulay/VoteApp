vagrant up
vagrant ssh manager1
git clone https://github.com/claude-boulay/VoteApp.git
docker swarm init --advertise-addr 192.168.99.100
//copier la commande pour ajouter des worker
exit
vagrant ssh worker1
//colle la commande copier précédemment
exit
vagrant ssh worker2
//colle la commande copier précédemment
exit
vagrant ssh manager1
cd VoteApp/voting-app/
docker network create --driver overlay vote
docker stack deploy -c docker-compose.yml vote-app

docker service ls "affichage des service"
docker service ps "nom du service voulue"

Pour reset 
être dans le dossier voting-app
cd VotingApp/voting-app
./script.bash
